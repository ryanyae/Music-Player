package model.songstate;

// Represents if the song is currently in one of the two given states.
// This enum was created because a song can only be in one of 2 states, PLAYING or PAUSED
public enum State {
    PLAYING,        // song is currently playing
    PAUSED          // song is currently paused
}
